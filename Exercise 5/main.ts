let famous_person:string="Hazrat Ali ibne abi talib";
let Quote : string="A wise man is he who knows his own ignorance.";
let message: string=`${famous_person} once said, "${Quote}"`;
console.log(message);
